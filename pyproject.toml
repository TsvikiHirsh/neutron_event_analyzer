[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "neutron_event_analyzer"
version = "0.4.0"
description = "Associate neutron event camera data across reconstruction tiers"
readme = "README.md"
authors = [{ name = "Tsviki Y. Hirsh", email = "tsviki.hirsh@gmail.com" }]
license = { text = "MIT" }
requires-python = ">=3.8"
dependencies = [
    "pandas>=1.5.0",
    "numpy>=1.21.0",
    "tqdm>=4.62.0",
    "scipy>=1.7.0",
]

[project.optional-dependencies]
ml = ["scikit-learn>=1.0.0", "torch"]
dev = ["pytest>=7.0.0", "pytest-cov>=4.0.0"]

[project.urls]
"Homepage" = "https://github.com/TsvikiHirsh/neutron_event_analyzer"
"Repository" = "https://github.com/TsvikiHirsh/neutron_event_analyzer"

[project.scripts]
nea-assoc = "neutron_event_analyzer.cli:main_assoc"
